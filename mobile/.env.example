# Production API (Bearer must be deployed — Phase 0).
EXPO_PUBLIC_API_URL=https://api.mindkeep.cloud

# Local API from a physical phone: use the computer LAN IP, not localhost.
# EXPO_PUBLIC_API_URL=http://192.168.1.10:4000
